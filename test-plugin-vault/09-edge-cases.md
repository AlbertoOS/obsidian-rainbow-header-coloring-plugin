# 09 — Edge Cases

Unusual or boundary conditions.

## Heading immediately at top of file

(This file's own `# 09 — Edge Cases` is the test.)

## Heading with only whitespace after the hash

#   (Extra spaces before text)

##	(Tab after hash)

## Very long heading

# This Is An Extremely Long Heading That Goes On And On And On And Contains Many Words And Should Still Get The Full Background Color Treatment Without Any Wrapping Issues Or Truncation

## Heading with special characters

### C++ templates & `std::vector<int>`

### Price: $19.99 (or €17.99)

### 100% complete — no errors!

### Query: SELECT * FROM `headers` WHERE level < 3;

## Heading with numbers only

### 2024

### 42

### 3.14

## Setext-style headings (underline syntax)

Setext H1
=========

Setext H2
---------

These use `=` / `-` underlines instead of `#`. Verify coloring applies.

## Heading right before end of file with no trailing newline

###### Last heading — no blank line after
